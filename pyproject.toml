[project]
name = "lead-details-mcp"
version = "0.1.0"
description = "Lead Generation MCP Server with email support"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.12.3",
    "python-dotenv>=1.0.0",
]

[tool.fastmcp]
file = "main.py"
